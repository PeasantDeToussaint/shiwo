#!/usr/bin/env node
/**
 * Alias entry: same as generate-quiz-lean.js (hyphenated name).
 * Use either:
 *   node scripts/generate-quiz-lean.js --topic="..."
 *   node scripts/generate_quiz_lean.js --topic="..."
 */
require("./generate-quiz-lean.js");
